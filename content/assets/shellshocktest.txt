#!/bin/bash
(
echo "CVE-2014-6271 This should NOT echo the word vulnerable below"
env x='() { :;}; echo vulnerable' bash -c "echo this is a test"
echo "CVE-2014-7169 This should output the word date, not the current time and date"
if [ -f /tmp/echo ]
then
   echo "There is already a file named /tmp/echo. Test incomplete."
      exit 1
fi
cd /tmp; env X='() { (a)=>\' bash -c "echo date"; cat echo
  rm /tmp/echo
) 2>/dev/null
